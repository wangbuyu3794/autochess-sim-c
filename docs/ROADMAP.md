# 版本路线

本路线用于框定项目范围，防止开发无限扩张。实际开发过程中可以根据学习效果和实现难度调整。

## V0.1 Battle Simulator

目标：完成最小战斗模拟。

包含：

- 基础 CMake 工程；
- 英雄和战斗单位结构；
- 双方固定 3 个单位；
- 普通攻击；
- 当前生命值最低目标选择；
- 生命值和死亡；
- 胜负判断；
- 中文战斗日志；
- 基础测试。

不包含：

- 商店；
- 经济；
- 棋盘部署；
- 备战席；
- 羁绊；
- 技能；
- 护甲、魔抗、法力值、暴击率和暴击伤害；
- AI 运营；
- 存档；
- 图形界面。

学习重点：结构体、数组、函数拆分、基础测试。

说明：`V0.1` 的目标选择是临时策略。后续 `V0.2` 加入棋盘坐标后，再升级到按距离最近索敌。

## V0.2 Board And Movement

目标：加入棋盘和简单移动。

包含：

- 8x7 方格棋盘；
- 双方半场；
- 战斗单位坐标；
- 攻击距离；
- 距离最近索敌；
- 简单贪心移动；
- 越界和占位检查。

学习重点：二维数组、坐标、边界检查。

当前实现说明：

- `board` 模块负责坐标、部署区域和曼哈顿距离；
- `battle` 模块负责战斗单位占位、索敌、攻击和移动；
- 移动暂时采用贪心规则，不做复杂寻路。

## V0.3 Player And Bench

目标：加入玩家和备战席。

包含：

- 玩家结构；
- 备战席；
- 单位部署、撤下和移动；
- 上场人数限制；
- 已部署单位复制到战斗上下文。

学习重点：模块接口、状态管理。

当前实现说明：

- `Unit` 表示玩家拥有的单位；
- `Player` 管理备战席、部署区域、等级上场限制；
- 战斗开始前，已部署 `Unit` 会被复制成 `BattleUnit`；
- 终端命令解析延后到后续版本实现。

## V0.4 Shop And Economy

目标：加入商店和经济系统。

包含：

- 金币；
- 回合收入；
- 利息；
- 商店刷新；
- 购买英雄；
- 等级抽卡概率；
- 购买经验和升级。

学习重点：随机数、概率、资源系统。

当前实现说明：

- 每回合收入为基础收入加利息；
- 利息按每 10 金币 1 金币计算，最多 5 金币；
- 刷新商店消耗 2 金币；
- 购买经验消耗 4 金币，获得 4 经验；
- 当前每 4 经验升 1 级，最高 8 级；
- 商店每次刷新 5 个英雄；
- 购买英雄会扣除金币，并把单位加入备战席。

## V0.5 Merge System

目标：加入升星。

包含：

- 三个相同一星合成二星；
- 三个相同二星合成三星；
- 属性倍率；
- 自动合成测试。

学习重点：数组查找、删除、合并、边界测试。

当前实现说明：

- 添加单位到备战席后会自动检查合成；
- 三个同模板同星级单位会合成一个高一星单位；
- 九个一星单位可以连续合成为一个三星单位；
- 合成暂时不压缩 `units` 数组，而是把被消耗单位标记为 inactive；
- 进入战斗时，根据星级放大战斗单位属性。

## V0.6 Trait System

目标：加入羁绊。

包含：

- 职业和阵营；
- 羁绊统计；
- 阈值判断；
- 属性加成。

学习重点：枚举、计数表、数据驱动设计。

当前实现说明：

- 英雄模板拥有职业和阵营两个羁绊字段；
- `trait` 模块负责羁绊统计和阈值计算；
- 战斗开始前统一应用羁绊；
- 当前已实现守卫生命值加成和剑士攻击力加成；
- 游侠、法师和阵营羁绊的具体效果延后。

## V0.7 Skills And Mana

目标：加入法力和技能。

包含：

- 护甲和魔抗；
- 攻击回蓝；
- 最大法力值和当前法力值；
- 技能释放；
- 暴击率和暴击伤害；
- 单体伤害；
- 群体伤害；
- 护盾；
- 治疗或控制的基础版本。

学习重点：技能分发、函数指针或策略表。

当前实现说明：

- 英雄模板拥有初始法力、最大法力和技能编号；
- 普通攻击后获得法力值；
- 法力值满后释放技能并清空法力；
- 当前实现了自我治疗、额外攻击伤害和固定单体伤害；
- 群体技能、控制和更完整的护盾机制延后。

## V0.8 Operation AI

目标：加入电脑运营 AI。

包含：

- AI 购买；
- AI 刷新；
- AI 升星；
- AI 自动布阵；
- 阵容评分。

学习重点：评分函数、简单决策系统。

当前实现说明：

- AI 可以结算收入；
- AI 可以刷新商店；
- AI 会购买可负担的最高评分英雄；
- AI 会把备战席中评分最高的单位部署到己方半场预设位置；
- AI 通过现有规则接口行动，不绕过金币、部署区域和等级限制；
- 羁绊匹配、升星优先级和更复杂刷新策略延后。

## V0.9 Full Match Loop

目标：完成多回合对局。

包含：

- 主状态机；
- 多回合流程；
- 玩家生命值；
- 胜负扣血；
- 游戏结束。

学习重点：状态机、主循环、流程控制。

当前实现说明：

- `game` 模块负责对局级流程；
- 每回合会执行准备、战斗、结算；
- 胜方根据存活单位数量对败方扣血；
- 玩家或敌方生命值归零后游戏结束；
- 当前最多运行固定回合数，超过则判定平局。

## V1.0 Terminal Release

目标：形成可发布的终端正式版。

包含：

- 启动参数帮助；
- 版本号输出；
- 自定义随机种子；
- 自定义最大回合数；
- 静默运行模式；
- 中文对局摘要；
- README 和文档补齐；
- 测试补齐；
- 平衡性调整。

不包含：

- 交互式命令系统；
- 保存和读取；
- 外部数据文件；
- 图形界面。

学习重点：命令行参数、工程整理、发布准备。

当前实现说明：

- `autochess --help` 可以查看可用选项；
- `autochess --version` 可以查看版本；
- `--player-seed` 和 `--enemy-seed` 用于复现商店随机结果；
- `--rounds` 用于限制最大对局回合数；
- `--quiet` 用于只查看最终结算。

存档、外部数据文件和交互式命令系统移动到后续 `V1.x` 扩展，不作为第一阶段终点的阻塞项。

## V1.1 Interactive Preparation

目标：让玩家在准备阶段开始参与决策。

包含：

- `--interactive` 启动参数；
- 准备阶段命令循环；
- 查看商店；
- 查看阵容；
- 购买英雄；
- 刷新商店；
- 自动部署；
- 确认进入战斗；
- 命令模块测试。

不包含：

- 手动坐标部署；
- 出售单位；
- 锁定商店；
- 保存和读取；
- 外部数据文件。

学习重点：字符串解析、命令分发、输入循环、模块复用。

当前实现说明：

- `command` 模块只负责解析和分发命令；
- 购买和刷新仍然调用 `shop` 模块；
- 自动部署仍然调用 `ai` 模块；
- 交互模式每回合由玩家准备、敌方 AI 准备、战斗结算组成。

## V1.2 Manual Deployment

目标：让玩家能控制基础站位。

包含：

- `deploy <备战席> <行> <列>`；
- `move <旧行> <旧列> <新行> <新列>`；
- `recall <行> <列>`；
- 命令错误提示；
- 非法部署测试。

不包含：

- 出售单位；
- 锁定商店；
- 装备；
- 完整手动运营 AI 对抗；
- 保存和读取。

学习重点：多参数命令解析、坐标校验、复用模块规则、边界测试。

当前实现说明：

- `deploy` 调用 `player_deploy_from_bench`；
- `move` 调用 `player_move_deployed_unit`；
- `recall` 调用 `player_return_unit_to_bench`；
- 所有命令都会复用 `player_result_name` 输出中文结果。

## V1.3 Economy Commands

目标：补齐基础运营经济操作。

包含：

- `sell <编号>`；
- `buyxp`；
- 出售金币返还；
- 经验购买和升级；
- 状态展示下回合收入；
- 命令模块测试。

不包含：

- 连胜/连败奖励；
- 锁定商店；
- 共享卡池；
- 复杂等级经验表；
- 出售确认弹窗。

学习重点：状态修改封装、资源流转、命令错误处理、经济规则测试。

当前实现说明：

- `player_sell_unit` 负责出售单位并发放退款；
- 退款按星级折算，1星/2星/3星分别相当于 1/3/9 个原价单位；
- `buyxp` 调用 `economy_buy_experience`，继续复用经济模块规则；
- 状态展示会显示经验进度和下回合收入。

## V1.4 Shop Lock And Round UX

目标：补齐商店锁定，并让回合开始更清晰。

包含：

- `lock` 命令；
- 商店锁定状态；
- 回合开始免费刷新；
- 锁定商店后下回合保留；
- 回合开始收入提示；
- 商店刷新/保留状态提示。

不包含：

- 商店锁定 UI 高亮；
- 商店概率展示；
- 共享卡池；
- 连胜/连败奖励。

学习重点：状态持久化、回合边界、命令和主循环协作。

当前实现说明：

- `Shop` 保存 `is_locked`；
- `shop_prepare_round` 负责判断刷新或保留；
- `lock` 命令切换锁定状态；
- 手动 `refresh` 会刷新并清除锁定。

## V1.5 Shop Odds And Hero Pool

目标：让玩家能理解商店随机结果。

包含：

- `odds` 命令；
- `pool` 命令；
- 商店概率查询接口；
- 英雄池展示；
- 概率测试。

不包含：

- 3费、4费英雄；
- 共享卡池；
- 每个英雄剩余数量；
- 商店概率图形化展示。

学习重点：数据查询接口、展示与规则复用、避免重复概率表。

当前实现说明：

- `shop_get_cost_probability` 提供费用概率；
- 抽卡逻辑和 `odds` 展示使用同一套概率；
- `pool` 直接读取 `hero_get_templates`。

## V1.6 Expanded Hero Pool

目标：扩展英雄费用层级。

包含：

- 3 费英雄；
- 4 费英雄；
- 1-4 费商店概率；
- `odds` 显示 1-4 费；
- 高费英雄池测试。

不包含：

- 5 费英雄；
- 新技能类型；
- 共享卡池；
- 英雄数量上限。

学习重点：数据扩展、概率表、向后兼容测试。

当前实现说明：

- 新增岚弩、符法、曦盾、夜刺；
- 概率表仍在 `shop_get_cost_probability` 中统一维护；
- 商店抽卡按概率累计选择费用，再从对应费用英雄中随机选择。

## V1.7 Trait Insight

目标：让玩家理解当前阵容羁绊。

包含：

- `traits` 命令；
- 羁绊数量统计；
- 已触发档位；
- 下一档差距；
- 守卫和剑士加成说明。

不包含：

- 新羁绊战斗效果；
- 羁绊推荐购买；
- 自动阵容规划；
- 复杂阵营效果。

学习重点：统计汇总、解释性输出、规则和展示分离。

当前实现说明：

- 命令层根据已上场单位统计羁绊；
- `trait_current_threshold` 和 `trait_next_threshold` 提供档位解释；
- 暂未开放效果的羁绊会明确提示“仅统计”。

## V1.8 More Trait Effects

目标：让更多羁绊真正影响战斗。

包含：

- 游侠射程加成；
- 法师初始法力加成；
- 元素生命加成；
- `traits` 命令展示新增效果；
- 羁绊效果测试。

不包含：

- 城邦效果；
- 森林效果；
- 暗影效果；
- 持续光环；
- 战斗中动态羁绊变化。

学习重点：规则扩展、战斗前属性应用、效果测试。

当前实现说明：

- 新效果仍然在 `battle_apply_trait_summary` 中统一应用；
- `trait` 模块提供对应数值 helper；
- 命令层只读取 helper 显示说明。

## V1.9 Trait Completion

目标：补齐当前所有羁绊的第一版战斗效果。

包含：

- 城邦初始法力加成；
- 森林生命加成；
- 暗影攻击加成；
- `traits` 命令展示所有现有羁绊效果；
- 职业和阵营羁绊叠加测试。

不包含：

- 护甲；
- 魔抗；
- 暴击；
- 攻击速度；
- 战斗中动态光环。

学习重点：规则补完、组合叠加、测试预期设计。

当前实现说明：

- 仍然由 `battle_apply_trait_summary` 在战斗开始前应用；
- 城邦、森林、暗影各自拥有独立 helper；
- 多个羁绊可以按当前顺序叠加到同一个单位身上。

## V1.10 Battle Attributes

目标：加入基础战斗词条，让伤害系统从“直接扣攻击力”升级为“经过属性计算”。

包含：

- 护甲；
- 魔抗；
- 暴击率；
- 暴击伤害；
- 普通攻击护甲减伤；
- 法术技能魔抗减伤；
- 商店和英雄池展示新增属性；
- 伤害公式测试。

不包含：

- 攻击速度；
- 闪避；
- 真实随机暴击；
- 装备系统；
- 复杂技能系数。

学习重点：结构体扩展、公式封装、可测试的战斗计算。

当前实现说明：

- `HeroTemplate` 保存基础属性；
- `BattleUnit` 保存战斗中的属性快照；
- `battle_calculate_attack_damage` 统一计算普通攻击伤害；
- `battle_calculate_spell_damage` 统一计算法术伤害；
- 暴击使用确定性检定，避免测试结果漂移。

## V1.11 Skill Definitions

目标：把技能系统从硬编码分支整理成可扩展的数据定义。

包含：

- 技能目标类型；
- 技能伤害类型；
- 基础伤害；
- 攻击系数；
- 治疗量；
- 商店和英雄池显示技能名称；
- 技能定义测试。

不包含：

- 新英雄；
- 新技能数量扩展；
- 多目标技能；
- 范围技能；
- 控制效果。

学习重点：数据驱动、接口设计、行为保持兼容。

当前实现说明：

- `SkillDefinition` 保存技能规则；
- `skill_get_definition` 查询技能定义；
- `skill_calculate_damage` 根据物理/法术类型调用伤害公式；
- `skill_cast` 只负责检查施法条件、记录日志和应用结果。

## V1.12 Expanded Skills

目标：用技能定义表做出更有区分度的英雄技能。

包含：

- 秘法箭；
- 圣光守护；
- 终结斩；
- 低血额外伤害字段；
- 高费英雄技能替换；
- 新技能数值测试。

不包含：

- 范围伤害；
- 多目标治疗；
- 护盾状态；
- 控制效果；
- 技能动画。

学习重点：在已有抽象上扩展规则、保持兼容、验证新机制。

当前实现说明：

- 秘法箭复用基础伤害和攻击系数字段；
- 圣光守护复用自身治疗字段；
- 终结斩使用新增的低血阈值和额外伤害字段。

## V1.13 Battle Log Details

目标：让战斗日志能解释新增战斗属性和技能结果。

包含：

- 暴击标记；
- 技能伤害结果；
- 技能治疗结果；
- 更清晰的战斗过程观察。

不包含：

- 完整公式拆解；
- 每次减伤前后对比；
- 战斗回放；
- UI 事件系统。

学习重点：展示层与战斗逻辑分离、可读性改进、行为不变的重构。

当前实现说明：

- `logger_attack_detail` 负责攻击日志；
- `logger_skill_damage` 负责技能伤害日志；
- `logger_skill_heal` 负责技能治疗日志；
- 战斗和技能模块只传递计算后的结果。

## V1.14 Status Effects

目标：加入第一版战斗状态，让技能机制继续扩展。

包含：

- 护盾；
- 灼烧；
- 眩晕；
- 状态行动前结算；
- 技能附加状态；
- 状态日志；
- 状态效果测试。

不包含：

- 通用 buff 列表；
- 状态驱散；
- 状态来源追踪；
- 多层同类状态叠加规则；
- 复杂控制抗性。

学习重点：状态数据设计、行动前结算、规则和日志协作。

当前实现说明：

- 状态字段直接放在 `BattleUnit`；
- 护盾在 `battle_apply_damage` 中先于生命值消耗；
- 灼烧和眩晕由 `battle_process_status_start` 在单位行动前结算；
- 技能定义表提供状态数值。

## V1.15 Targeted Skills

目标：让技能可以选择友军目标，并支持基础多目标伤害。

包含：

- 最低血友军目标；
- 主目标相邻溅射；
- 圣光守护目标规则升级；
- 火球术溅射伤害；
- 目标规则测试。

不包含：

- 任意范围模板；
- 全体治疗；
- 链式弹射；
- 复杂目标优先级配置。

学习重点：目标选择、比例比较、多目标应用。

当前实现说明：

- `SKILL_TARGET_LOWEST_HP_ALLY` 选择同侧生命比例最低的存活单位；
- `splash_percent` 控制对主目标相邻敌人的溅射伤害；
- 溅射使用缩放后的技能定义重新计算伤害。

## V1.16 Equipment System

目标：加入第一版装备系统，让准备阶段选择能影响战斗属性。

包含：

- 装备定义表；
- 玩家装备库存；
- 查看装备命令；
- 给单位装备命令；
- 战斗开始时应用装备属性；
- 出售和升星时回收装备；
- 装备测试。

不包含：

- 装备掉落；
- 装备合成；
- 多装备槽；
- 装备主动技能；
- 装备推荐。

学习重点：资源库存、模板数据、实例状态、战斗快照转换。

当前实现说明：

- `EquipmentTemplate` 描述装备名称和属性加成；
- `Player` 保存每种装备的库存数量；
- `Unit` 保存当前装备编号；
- `BattleUnit` 保存战斗中的装备编号，并在复制入场时应用属性；
- 当前开放长剑、守护甲、法力宝石和暴击手套四件基础装备。

## V1.17 Equipment Rewards

目标：让装备库存能在对局过程中增长，形成准备阶段的新决策来源。

包含：

- 回合装备奖励；
- 胜方获得装备；
- 稳定的奖励轮换规则；
- 奖励日志；
- 奖励库存测试。

不包含：

- 随机掉落池；
- 装备合成；
- 多件装备同时装备；
- AI 主动装备；
- 装备稀有度。

学习重点：对局结算、奖励发放、确定性规则、长期资源增长。

当前实现说明：

- `game_select_round_equipment_reward` 根据回合数稳定选择奖励；
- 奖励顺序为长剑、守护甲、法力宝石、暴击手套，并循环；
- `game_settle_battle` 在胜负扣血后给胜方增加装备库存；
- 平局不会发放装备奖励。

## V1.18 AI Equipment Usage

目标：让装备库存自动参与战斗，减少玩家必须手动装备的负担。

包含：

- AI 自动装备；
- `auto` 命令自动装备；
- `ready` 命令自动装备；
- 电脑准备阶段自动装备；
- 自动装备测试。

不包含：

- 按英雄职业定制装备；
- 按敌方阵容反制装备；
- 多装备槽；
- 装备推荐说明；
- 卸下装备命令。

学习重点：自动决策、复用玩家规则接口、避免绕过库存约束。

当前实现说明：

- `ai_equip_best_units` 只给已上场且未装备的单位装备；
- 装备分配优先选择 AI 评分最高的上场单位；
- 装备仍然通过 `player_equip_unit` 应用，因此会正常消耗库存；
- 自动逻辑不改写手动装备过的单位。

## V1.19 Equipment Management

目标：补齐第一版装备管理，让玩家可以主动调整已经穿上的装备。

包含：

- 手动卸装接口；
- `unequip <单位编号>` 命令；
- 卸装回库存；
- 无装备错误提示；
- 卸装测试。

不包含：

- 指定装备槽；
- 多装备槽；
- 装备拖拽式交换；
- 卸装费用；
- 自动重新分配所有装备。

学习重点：状态回滚、资源归还、命令错误处理。

当前实现说明：

- `player_unequip_unit` 负责把单位装备放回玩家库存；
- 没有装备的单位会返回“单位没有装备”；
- 命令层只解析单位编号并复用玩家层规则；
- 卸装后可以重新使用 `equip` 或 `auto` 分配装备。

## V1.20 Equipment Fit AI

目标：让自动装备从“随便给强单位”升级为“根据单位定位分配装备”。

包含：

- 装备适配评分；
- 攻击装偏向输出单位；
- 防御装偏向守卫和高坦度单位；
- 法力装偏向有技能收益的单位，尤其法师；
- 暴击装偏向高攻击、高暴击或暗影/剑士单位；
- 适配评分测试。

不包含：

- 阵容级出装规划；
- 根据敌方阵容反制；
- 装备套装；
- 装备稀有度；
- 玩家出装推荐文本。

学习重点：评分函数、局部最优分配、规则可测试化。

当前实现说明：

- `ai_score_equipment_for_unit` 根据英雄模板和装备编号计算适配分；
- `ai_equip_best_units` 会在所有可用装备和未装备上场单位中选择最高分组合；
- 每次装备后重新计算下一组组合；
- 手动装备过的单位仍不会被自动覆盖。

## V1.21 Equipment Insight

目标：让玩家能看到装备适配评分，理解自动装备为什么这样分配。

包含：

- `equipfit <单位编号>` 命令；
- 展示指定单位对各装备的适配分；
- 同时展示当前装备库存；
- 非法单位编号错误提示；
- 命令测试。

不包含：

- 全阵容装备推荐；
- 自动给出最优出装方案；
- 图形化评分条；
- 详细公式拆解。

学习重点：解释性输出、调试命令、复用 AI 评分函数。

当前实现说明：

- `equipfit` 直接调用 `ai_score_equipment_for_unit`；
- 命令输出使用真实 AI 评分，避免展示规则和自动装备规则不一致；
- 这个命令主要用于学习、调试和后续平衡调整。

## V1.30 Terminal Milestone

目标：收束终端版第一阶段，形成可演示、可测试、可继续扩展的完整小型项目。

包含：

- 完整自动战斗；
- 棋盘、移动和站位；
- 玩家、备战席、部署、移动和撤回；
- 商店、经济、经验和锁店；
- 升星；
- 羁绊；
- 技能、法力和状态；
- 装备、装备奖励、装备管理和装备适配查看；
- AI 购买、部署和装备；
- 交互式准备阶段；
- 中文终端日志；
- 基础测试；
- `guide` 推荐操作流程。

不包含：

- 图形界面；
- 联网或八人对局；
- 复杂装备合成；
- 存档读取；
- 外部数据文件驱动；
- 商业级平衡性。

当前实现说明：

- `V1.30` 是终端版第一阶段终点；
- 后续新增大功能应作为 `V2.x` 或独立扩展规划；
- 进入图形界面前，应优先保持当前测试全绿。

## V1.31 Showcase Polish

目标：把终端版整理成更容易展示和讲解的作品状态。

包含：

- README 快速体验；
- README 功能亮点；
- README 学习重点；
- README 项目结构说明；
- `docs/SHOWCASE.md` 作品展示说明；
- 推荐演示命令。

不包含：

- 新玩法；
- 新战斗规则；
- 图形界面；
- 大规模代码重构。

当前实现说明：

- 这一版只做展示整理，不改变核心规则；
- `SHOWCASE.md` 面向“给别人看项目”；
- README 面向“第一次打开仓库的人”。

## V2.0 Graphical Version

目标：使用 raylib 制作图形版。

包含：

- 窗口；
- 棋盘绘制；
- 鼠标操作；
- 英雄卡牌；
- 血条和法力条；
- 简单动画。

启动前置条件：

- 终端版保持可构建；
- 测试保持全绿；
- 图形版作为独立入口，不破坏终端版；
- 先完成 `docs/GUI_DESIGN.md` 中的窗口布局和交互边界。

第一批拆分：

- `V2.1 GUI Skeleton`：新增窗口和空棋盘；
- `V2.2 Board View`：绘制棋盘、单位、备战席和商店；
- `V2.3 Preparation Interaction`：点击购买、部署、移动、撤回和开始战斗；
- `V2.4 Battle Visualization`：显示战斗结果和关键日志；
- `V2.5 UI Polish`：改善卡牌、颜色和提示。

## V2.1 GUI Skeleton

目标：建立图形版独立入口，验证 raylib 窗口、基础布局和 CMake 可选构建。

包含：

- `AUTOCHESS_BUILD_GUI` CMake 开关；
- `autochess_gui` 可执行目标；
- raylib 窗口；
- 顶部状态栏；
- 8x7 空棋盘；
- 左侧信息面板；
- 右侧详情面板；
- 底部商店预览；
- 不影响终端版默认构建。

不包含：

- 鼠标交互；
- 单位真实绘制；
- 备战席真实绘制；
- 战斗动画；
- 中文字体加载。

当前实现说明：

- 默认构建不开启 GUI；
- 开启 GUI 时需要本机已安装 raylib；
- 终端版 `autochess` 和测试目标保持原样；
- 下一步进入 `V2.2 Board View`，把真实单位和备战席画出来。

## V2.2 Board View

目标：让图形版窗口开始展示真实游戏状态，而不是只画空布局。

包含：

- 从 `GameContext` 读取演示局面；
- 绘制玩家和敌方已部署单位；
- 绘制单位模板编号、星级和装备标记；
- 绘制备战席预览；
- 绘制商店预览；
- 绘制一个单位详情预览；
- 保持只读展示。

不包含：

- 鼠标点击；
- 购买、部署、移动操作；
- 战斗动画；
- 中文字体加载；
- 完整 UI 美化。

当前实现说明：

- 图形版启动时创建一个演示局面；
- 单位以 `H#` 显示英雄模板编号，避免在默认 raylib 字体下显示中文失败；
- `E` 表示单位已装备；
- 下一步进入 `V2.3 Preparation Interaction`，把点击输入接到现有规则接口。

## V2.3 Preparation Interaction

目标：让图形版准备阶段从只读预览升级为可点击操作。

包含：

- 点击商店卡牌购买英雄；
- 点击备战席单位进行选中；
- 点击己方棋盘空格部署备战席单位；
- 点击己方已上场单位进行选中；
- 点击己方棋盘空格移动已上场单位；
- 点击空备战席撤回已上场单位；
- 按钮支持刷新商店、锁定商店、自动部署/装备和进行一场战斗；
- 底部消息栏显示最近一次操作结果。

不包含：

- 拖拽操作；
- 装备手动点击穿戴；
- 中文字体加载；
- 战斗动画和逐帧回放；
- 商业级 UI 美化。

当前实现说明：

- GUI 层新增轻量 `GuiState`，只记录当前选中对象和提示消息；
- 鼠标操作会调用现有 `shop`、`player`、`ai`、`game` 接口，不直接改写核心规则；
- 备战席现在绘制完整 9 格；
- 图形界面的提示文字暂时使用英文，避免默认 raylib 字体显示中文失败；
- 下一步进入 `V2.4 Battle Visualization`，把战斗过程或关键结果展示得更清楚。

## V2.4 Battle Visualization

目标：让图形版在执行战斗后给出清晰的战斗摘要，而不是只在底部显示一句结果。

包含：

- 记录最近一场战斗的回合数；
- 显示战斗胜负；
- 显示玩家和敌方生命值变化；
- 显示双方战斗前上场人数；
- 显示胜方获得的装备奖励；
- 战斗后如果游戏未结束，自动进入下一轮准备状态。

不包含：

- 逐帧战斗动画；
- 每次普通攻击和技能释放的完整日志；
- 单位实时血条变化；
- 暂停、继续和回放；
- 中文字体加载。

当前实现说明：

- GUI 层新增 `GuiBattleSummary`，只保存展示所需的战斗摘要；
- Battle 按钮会在调用 `game_run_battle_phase` 前后记录生命值、上场人数和结果；
- 右侧面板现在同时承担选中单位详情和最近战斗摘要展示；
- 装备奖励在 GUI 中暂用英文名显示，避免默认字体下的中文显示问题；
- 下一步进入 `V2.5 UI Polish`，改善卡牌、提示、布局和中文显示体验。

## V2.5 UI Polish

目标：把图形版从“功能可用”整理到“中文友好、信息更清楚”的状态。

包含：

- 加载中文字体；
- 中文化状态栏、信息面板、按钮、卡牌、单位详情和战斗摘要；
- 统一文本绘制入口，集中处理字体回退；
- 缩短商店和备战席卡牌文字，降低溢出风险；
- 操作反馈使用中文，并保留规则错误原因；
- 保持终端版和测试目标不受影响。

不包含：

- 复杂图标资产；
- 战斗动画；
- 拖拽操作；
- 装备手动点击穿戴；
- 多分辨率自适应布局；
- 商业级视觉设计。

当前实现说明：

- GUI 会优先尝试加载 `simhei.ttf`、`msyh.ttc`、`simsun.ttc`；
- 若中文字体加载失败，会回退到 raylib 默认字体，保证程序仍可运行；
- 文本绘制通过 `gui_draw_text` 统一处理；
- GUI 文案已从 V2.4 的英文提示改为中文提示；
- 下一步可以继续做 V2.6，例如装备点击穿戴，或者进入战斗日志/动画方向。

## V2.6 Equipment Interaction

目标：让图形版支持基础装备穿戴操作，使装备系统不再只依赖终端命令或自动装备。

包含：

- 在左侧信息面板显示装备库存；
- 点击装备库存项；
- 给当前选中的玩家单位穿戴装备；
- 支持替换已有装备，并复用玩家模块的库存回收逻辑；
- 显示未选择单位、库存不足、装备无效等操作反馈。

不包含：

- 装备拖拽；
- 装备合成；
- 装备详情弹窗；
- 多件装备同时穿戴；
- 装备推荐评分展示；
- 战斗动画。

当前实现说明：

- GUI 新增装备命中区域和 `gui_handle_equipment_click`；
- 装备穿戴调用 `player_equip_unit`，不直接修改玩家数据；
- 左侧装备库存会根据 `player_count_equipment` 显示数量；
- 库存为 0 的装备仍可见，但视觉置灰并由规则层返回库存不足；
- 下一步可以继续做装备详情、装备适配分展示，或者转向战斗日志可视化。

## V2.7 Equipment Insight

目标：把自动装备背后的适配评分展示到图形界面，让玩家能理解“为什么这件装备更适合当前单位”。

包含：

- 选中单位后显示每件装备的适配分；
- 高亮当前库存中评分最高的装备；
- 标记当前已穿戴装备；
- 继续支持点击装备进行穿戴或替换；
- 复用现有 AI 装备评分函数。

不包含：

- 新增装备评分规则；
- 详细公式拆解；
- 装备详情弹窗；
- 装备推荐排序列表；
- 装备拖拽或合成；
- 战斗动画。

当前实现说明：

- GUI 使用 `ai_score_equipment_for_unit` 计算适配分；
- 只有选中单位后才显示分数；
- 当前可用且分数最高的装备会绿色高亮；
- 当前已穿戴装备会金色高亮并显示“已穿”；
- 下一步可以做装备详情弹窗、评分解释，或转向战斗日志可视化。

## V2.8 Equipment Detail

目标：让玩家点击装备时能看到更完整的信息，不需要只依赖装备名和适配分猜测效果。

包含：

- 点击左侧装备库存后，在右侧显示装备详情；
- 显示装备库存数量；
- 显示生命、攻击、护甲、魔抗、初始法力、暴击率和暴击伤害加成；
- 选中单位时显示当前单位对该装备的适配分；
- 显示该装备主要适合的单位定位；
- 保留装备点击穿戴和替换行为。

不包含：

- 装备详情弹窗；
- 装备合成；
- 装备拖拽；
- 评分公式逐项拆解；
- 多件装备同时穿戴；
- 战斗动画。

当前实现说明：

- `GuiState` 新增当前关注装备，用于记住最近点击的装备；
- 装备详情读取 `EquipmentTemplate`，不复制新的装备属性数据；
- 适配分继续复用 `ai_score_equipment_for_unit`；
- 右侧详情面板会在有关注装备时显示装备详情，否则显示最近战斗摘要。

## V2.9 Battle Log View

目标：把战斗从“只显示结果”推进到“能看到最近过程”，让玩家理解单位移动、攻击、技能和结算是如何发生的。

包含：

- 点击战斗时捕获核心战斗日志；
- 在 GUI 右侧显示最近几条战斗过程；
- 显示移动、等待、攻击、暴击、技能、治疗、状态、击败和装备奖励等已有日志；
- 战斗后清空单位选择，让右侧优先展示战斗摘要和日志；
- 继续复用终端版日志输出接口。

不包含：

- 实时动画；
- 战斗逐步播放；
- 日志滚动条；
- 日志筛选；
- 独立 GUI 战斗事件系统；
- 修改战斗规则。

当前实现说明：

- `GuiState` 新增 `GuiBattleLog`，滚动保存最近几条日志；
- GUI 战斗按钮使用临时 `FILE` 捕获 `game_run_battle_phase` 输出；
- 日志显示只负责呈现，不参与战斗计算；
- 如果临时日志流创建失败，GUI 会给出日志捕获失败提示，但战斗仍可结算。

## V2.10 Unit Detail View

目标：让玩家选中单位后能直接理解这个棋子是谁、属于什么羁绊、技能大概做什么，而不是只看到编号和基础属性。

包含：

- 显示英雄中文名；
- 显示费用、星级和实例编号；
- 显示职业/阵营羁绊；
- 显示生命、攻击、射程、护甲、魔抗、暴击、法力；
- 显示技能名、伤害类型、目标类型和关键数值；
- 显示当前装备和位置。

不包含：

- 技能公式逐项拆解；
- 技能图标；
- 悬停浮层；
- 羁绊触发来源展开；
- 单位属性的装备后最终值预览。

当前实现说明：

- GUI 单位详情读取 `HeroTemplate`、`SkillDefinition` 和 `trait_name`；
- 选中单位时右侧优先显示单位详情；
- 未选中单位时右侧继续显示最近战斗摘要和日志；
- 装备详情仍通过点击左侧装备进入。

## V2.11 Action Guidance

目标：让 GUI 在演示和学习时更不容易迷路，用户能知道当前操作结果，也能看到下一步该做什么。

包含：

- 底部消息栏继续显示当前操作结果；
- 底部消息栏显示下一步建议；
- 根据备战席选择、棋盘选择、装备详情、已有战斗记录和游戏结束状态切换建议；
- 保留购买、部署、移动、装备和战斗的失败原因。

不包含：

- 教程弹窗；
- 任务清单系统；
- 高亮下一步可点击区域；
- 复杂新手引导；
- 自动替玩家做决策。

当前实现说明：

- 新增 `gui_next_action_hint`；
- 提示只读取当前 `GameContext` 和 `GuiState`；
- 提示不会修改游戏状态，也不参与规则判断。

## V2.12 Showcase Flow

目标：把当前成果整理成容易展示、容易验收的流程，避免项目在进入 V3.0 前继续发散。

包含：

- 更新 `docs/SHOWCASE.md`；
- 明确终端自动对局、终端交互和图形版三种演示方式；
- 写清楚图形版推荐点击顺序；
- README 当前阶段更新为图形版可演示状态；
- 标出 V3.0 前最后剩余工作。

不包含：

- 新增游戏规则；
- 新增 GUI 交互；
- 录制视频；
- 截图资产；
- 发布包制作。

当前实现说明：

- 展示文档已经覆盖终端版和图形版；
- 当前剩余主线收束为 GUI 小修、README/路线图最终收束和 V3.0 标记。

## V2.13 Showcase Polish

目标：在进入 V3.0 前做最后一轮轻量 GUI 展示打磨，减少演示时的文字拥挤。

包含：

- 缩短底部下一步建议文案；
- 调整下一步建议绘制位置；
- 更新左侧阶段标题和窗口标题；
- 保持构建、测试和现有交互稳定。

不包含：

- 新增规则；
- 新增交互；
- 改动核心模块；
- 复杂布局重做。

当前实现说明：

- `gui_next_action_hint` 的提示更短；
- 底部提示从中部右移，降低和操作结果重叠的概率；
- 这是 V3.0 前的 GUI 小修小补。
